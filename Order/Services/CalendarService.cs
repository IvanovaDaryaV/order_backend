/*using Microsoft.EntityFrameworkCore;
using Order;
using Order.Models;

namespace Order.Services
{
    public class CalendarService
    {
        private readonly ApplicationDbContext _context;
        public CalendarService(ApplicationDbContext context)
        {
            _context = context;
        }
        *//*public async GetCalendarById()
        {

        }*/
       /* public IEnumerable<Event> GetEventsByCalendarIdAndPeriod(int calendarId, DateTime startDate, DateTime endDate)
        {
            return _context.Events
                .Where(e => e.CalendarDate >= startDate && e.CalendarDate <= endDate)
                .ToList();
        }*//*

    }
}
*/